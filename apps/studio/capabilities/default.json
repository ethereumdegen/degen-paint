{
  "$schema": "../gen/schemas/desktop-schema.json",
  "identifier": "default",
  "description": "Window/event plumbing the Studio UI needs, plus the file picker its Import, Export and Open Project dialogs browse with.",
  "windows": ["main"],
  "permissions": ["core:default", "dialog:default"]
}
